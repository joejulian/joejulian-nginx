  location <%= location %> {
<% if @location_cfg_prepend -%><% location_cfg_prepend.sort.map do |key,value| -%>
    <%= key %> <%= value %>;
<% end -%><% end -%>
    root  <%= www_root %>;
    <% if @try_files then %>
      try_files <%= try_files %>;
    <% end %>
    index <% index_files.sort.map do |i| %> <%= i %> <% end %>; 
<% if @location_cfg_append -%><% location_cfg_append.sort.map do |key,value| -%>
    <%= key %> <%= value %>;
<% end -%><% end -%>
  }
